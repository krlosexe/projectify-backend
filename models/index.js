const {AuthModel}              = require('@models/Auth')
const {ProjectModel}           = require('@models/Projects')
const {ProjectReportWeekModel} = require('@models/ProjectsReportWeek')

module.exports = {
    AuthModel,
    ProjectModel,
    ProjectReportWeekModel
}

